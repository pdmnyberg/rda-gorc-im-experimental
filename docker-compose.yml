version: "3.8"

services:
  vite:
    user: "${UID:-1000}:${GID:-1000}"
    image: node:20-alpine
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "npm ci && npm run dev"
    environment:
      - CHOKIDAR_USEPOLLING=true
